/* ogl_render.h
 * Render given data to the currently active framebuffer.
 */
#pragma once

#include "ogl_mesh.h"

void tau_gra_ren_mesh(MESH* mesh);

// For rendering 
void tau_gra_ren_mesh_unitsquare(void);
